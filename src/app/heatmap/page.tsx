import Heatmap from "@/app/components/clients/heatmap";

export default function HeatmapPage() {
  return (
    <div className="min-h-screen bg-base-200 flex items-center justify-center">
      <Heatmap />
    </div>
  );
}
